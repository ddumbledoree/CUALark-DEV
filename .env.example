# OpenAI-compatible VLM provider. Do not commit real secrets in .env.
VLM_BASE_URL=
VLM_API_KEY=
VLM_MODEL=

# Phase 1 defaults. Use mock until desktop/VLM credentials are intentionally enabled.
CUA_OPERATOR=mock
CUA_TRACE_DIR=traces
